# Utils

Utility files contain helper functions that are reused in multiple places.

## Examples

- DOM helpers
- API helpers
- Storage helpers
- Validation helpers

## Rule of thumb

If a function is useful on multiple pages, it probably belongs in `utils/`.

If a function is only useful on one page, keep it in that page file.
